#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Alwasy 0 (success)
 */
int main(void)
{
	int num = 0;

	while (num <= 10)
	{
		printf("%d\n", num);
		num++;
	}
	return (0);
}
